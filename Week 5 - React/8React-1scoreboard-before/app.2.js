const header =(
    <header>
        <h1>My first react element!</h1>
        <p>I just learnt how to create a React node and render it into DOM, POWERFUL STUFF!</p>
    </header>
);
    console.log(title);

    ReactDOM.render(
        header,
        document.getElementById('root')
    );
    

    /*const title = <h1> My first react element!</h1> is JSX and is not javascript but a markup like syntax 
    removes the create element factor
    
    */